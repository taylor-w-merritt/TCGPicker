<%= form_for(@yugioh_set) do |f| %>
  <% if @yugioh_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@yugioh_set.errors.count, "error") %> prohibited this yugioh_set from being saved:</h2>

      <ul>
      <% @yugioh_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
